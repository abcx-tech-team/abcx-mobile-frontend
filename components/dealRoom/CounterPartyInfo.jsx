import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const CounterPartyInfo = ({ company }) => {
  return (
    <View>
      <Text>CounterPartyInfo</Text>
    </View>
  );
};

export default CounterPartyInfo;

const styles = StyleSheet.create({});
